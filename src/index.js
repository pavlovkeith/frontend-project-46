const genDiff = (filepath1, filepath2) =>
  // Заглушка для функции, возвращающая строку
  // eslint-disable-next-line implicit-arrow-linebreak
  `Comparing ${filepath1} and ${filepath2}`;
export default genDiff;
